Add a new MCP tool to the project manager server.

Tool name: $ARGUMENTS

Steps:
1. Add any needed database functions to db.py
2. Add the @server.tool() decorated function to server.py
3. Write a clear, specific tool description (this is LLM context — be precise)
4. Test by running: uv run python server.py

Follow existing patterns in server.py for consistency.
